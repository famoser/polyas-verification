name: PHP Composer

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  schedule:
    - cron: '0 0 * * 0'

permissions:
  contents: read

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
        with:
          # Fetch more than 1 commit so Scrutinizer can deduce where we are
          fetch-depth: 2

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.4'
          extensions: bcmath, gmp, openssl, pdo, zip

      - name: Print PHP config
        run: |
          php -v
          php --ini
          php -m
          php -i

      - name: Validate composer.json and composer.lock
        run: composer validate

      - name: Cache Composer packages
        id: composer-cache
        uses: actions/cache@v3
        with:
          path: vendor
          key: ${{ runner.os }}-php-${{ hashFiles('**/composer.lock') }}
          restore-keys: |
            ${{ runner.os }}-php-

      - name: Install dependencies
        run: composer install --prefer-dist --no-progress --no-scripts

      - name: Check dependencies
        run: composer audit

      - name: Lint
        run: |
          ./vendor/bin/phpcs

      - name: Types
        run: |
          ./vendor/bin/phpstan

      - name: Tests
        run: |
          ./vendor/bin/phpunit
